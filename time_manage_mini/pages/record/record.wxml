<!--pages/record/record.wxml-->
<view wx:for="{{recordList}}">
  {{item.beginTime}}|{{item.endTime}}|{{item.description}}|{{item.tag.color}}
</view>

<button class="mini-btn" type="primary" size="mini" bindtap="addRecord">+</button>